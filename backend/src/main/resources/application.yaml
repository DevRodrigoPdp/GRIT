spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  application:
    name: grit-backend
  jpa:
    open-in-view: false # Buena práctica: evitar consultas extra en la vista
  threads:
    virtual:
      enabled: true
  jackson:
    serialization:
      write-dates-as-timestamps: false
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 50MB  # Permitimos margen para varios archivos y el resto del JSON
      file-size-threshold: 2KB # Archivos mayores a esto se escriben temporalmente en disco para no saturar la RAM

server:
  port: ${PORT:8080}
  error:
    include-message: always # Útil para que el frontend sepa qué falló

application:
  security:
    jwt:
      expiration: 900000
      refresh-token:
        expiration: 604800000

management:
  endpoints:
    web:
      base-path: /management
      exposure:
        include: health, info, metrics # Métricas básicas para todos
  endpoint:
    health:
      probes:
        enabled: true
  info:
    env:
      enabled: true

